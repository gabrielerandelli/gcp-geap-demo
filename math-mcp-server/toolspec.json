{
  "tools": [
    {
      "name": "add",
      "description": "Calculate the sum of two numbers a and b.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "a": { "type": "number", "description": "The first number to add." },
          "b": { "type": "number", "description": "The second number to add." }
        },
        "required": ["a", "b"]
      },
      "annotations": {
        "title": "Add",
        "readOnlyHint": true,
        "idempotentHint": true
      }
    },
    {
      "name": "sub",
      "description": "Calculate the difference a - b.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "a": { "type": "number", "description": "The minuend number." },
          "b": { "type": "number", "description": "The subtrahend number to subtract." }
        },
        "required": ["a", "b"]
      },
      "annotations": {
        "title": "Subtract",
        "readOnlyHint": true,
        "idempotentHint": true
      }
    },
    {
      "name": "multiply",
      "description": "Calculate the product of a multiplied by b.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "a": { "type": "number", "description": "The first multiplier number." },
          "b": { "type": "number", "description": "The second multiplier number." }
        },
        "required": ["a", "b"]
      },
      "annotations": {
        "title": "Multiply",
        "readOnlyHint": true,
        "idempotentHint": true
      }
    },
    {
      "name": "divide",
      "description": "Calculate the quotient of a divided by b. Handles division by zero gracefully with recovery instructions.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "a": { "type": "number", "description": "The dividend number to divide." },
          "b": {
            "type": "number",
            "description": "The divisor number to divide by."
          }
        },
        "required": ["a", "b"]
      },
      "annotations": {
        "title": "Divide",
        "readOnlyHint": true,
        "idempotentHint": true
      }
    }
  ]
}

